apiVersion: v1
kind: Secret
metadata:
  name: ticketing-secrets
  namespace: ticketing
  labels:
    app.kubernetes.io/name: ticketing
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Redis password (à changer en production)
  REDIS_PASSWORD: "changeme"
  # TLS certificates (base64 encodés en production)
  # tls.crt: ""
  # tls.key: ""
